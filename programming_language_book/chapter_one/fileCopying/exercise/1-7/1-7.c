#include<stdio.h>

/* Printf the value of EOF */
int main(){

    /* EOF int -> -1 */
    /* EOF float -> 0.000 */
    /* EOF char -> 'blank'*/
    printf("EOF -> %d", EOF);

    return 0;
}